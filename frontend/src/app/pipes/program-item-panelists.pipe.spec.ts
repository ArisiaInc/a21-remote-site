import { ProgramItemPanelistsPipe } from './program-item-panelists.pipe';

describe('ProgramItemPanelistsPipe', () => {
  it('create an instance', () => {
    const pipe = new ProgramItemPanelistsPipe();
    expect(pipe).toBeTruthy();
  });
});
