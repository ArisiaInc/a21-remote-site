import { PersistantRoomLinkDirective } from './persistant-room-link.directive';

describe('PersistantRoomLinkDirective', () => {
  it('should create an instance', () => {
    const directive = new PersistantRoomLinkDirective();
    expect(directive).toBeTruthy();
  });
});
