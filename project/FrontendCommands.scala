object FrontendCommands {
  val npmInstall: String = "npm install"
  val angularInstall: String = "npm install --global @angular/cli"
  val devkitInstall: String = "npm install --save-dev @angular-devkit/build-angular"
  val test: String = "npm run test:ci"
  val serve: String = "npm run start"
  val devBuild: String = "npm run build:dev"
  val build: String = "npm run build:prod"
}
